import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // output: "export", // Commented out for development to allow client components
  reactCompiler: true,
};

export default nextConfig;
